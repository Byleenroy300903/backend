# --- HEDERA SETTINGS ---
# (Keeping placeholders so you can set these in Render's Dashboard for security)
hedera.account.id=${HEDERA_ACCOUNT_ID}
hedera.private.key=${HEDERA_PRIVATE_KEY}
hedera.network=testnet

# --- APEXPAY ORACLE ---
apexpay.ai.oracle.id=${ORACLE_ID}

# --- DATABASE CONNECTION (Standard Postgres) ---
# Format: jdbc:postgresql://[host]:[port]/[database]
spring.datasource.url=jdbc:postgresql://ep-raspy-mud-aiyyfzeq-pooler.c-4.us-east-1.aws.neon.tech:5432/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_7APZK8TFaXyq

# --- JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- SERVER SETTINGS ---
# Render provides the PORT variable; Spring Boot listens to it automatically
server.port=${PORT:8080}
